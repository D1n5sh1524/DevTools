# FastAPI Rules

- Use APIRouter.
- Use dependency injection.
- Use async endpoints where appropriate.
- Validate all input with Pydantic.
- Separate API, service, repository, and model layers.
- Add tests for every endpoint.
